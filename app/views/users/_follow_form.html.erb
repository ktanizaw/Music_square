<div id="follow_form_<%= user.id %>">
  <% if user != current_user %>
  <% unless current_user.following?(user) %>
    <%= form_with(model: current_user.active_relationships.build(followed_id: user.id)) do |f| %>
      <%= f.hidden_field :followed_id %>
      <%= f.submit "フォロー",class:"btn btn-outline-warning rounded-pill waves-effect submit-btn" %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.active_relationships.find_by(followed_id: user.id), method: :delete ) do |f| %>
      <%= f.submit "フォロー解除",class:"btn btn-outline-danger rounded-pill waves-effect submit-btn" %>
    <% end %>
  <% end %>
  <% end %>
</div>
